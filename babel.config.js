// Used only by Jest's `react-native` preset for component render tests.
// react-native-builder-bob compiles `src` with its own preset and ignores this.
module.exports = {
  presets: ['module:@react-native/babel-preset'],
};
